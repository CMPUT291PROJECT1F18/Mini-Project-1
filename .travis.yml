# ..travis.yml
dist: trusty

sudo: required

language: python
python: 3.6

matrix:
    include:
        - python: 3.6
          env: TOXENV=py36

before_install: 'pip install --upgrade pip'

install: 'pip install tox'

script:
    - 'tox'
